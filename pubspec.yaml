name: d_crypto_lite
description: "A dantown app clone"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  default_ui_components:
    git:
      url: https://github.com/thedevsaxist/default_ui_components
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  get_it: ^8.0.3
  go_router: ^15.1.1
  google_fonts: ^6.2.1
  provider: ^6.1.5

dev_dependencies:

  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/dantown_logo.png
    - .env
