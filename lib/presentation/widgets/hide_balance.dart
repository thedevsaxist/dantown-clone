import 'package:flutter/material.dart';

class HideBalance extends StatefulWidget {
  const HideBalance({super.key});

  @override
  State<HideBalance> createState() => _HideBalanceState();
}

class _HideBalanceState extends State<HideBalance> {
  @override
  Widget build(BuildContext context) {
    return Row(children: [
      
    ]);
  }
}
